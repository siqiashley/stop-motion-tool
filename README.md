# 4-12 FPS Stop Motion Tool

Open `index.html` in a browser to use the tool.

It has two modes:

- Convert one uploaded video into a low-frame-rate MP4.
- Convert multiple uploaded photos into a stop-motion MP4, ordered by filename.
